//! halter CLI support library for the `halter` (server) binary. The server config lives
//! in [`config`]. The consumer-side provision fetch + native config writers now live in
//! the standalone `halter-agent` crate.

pub mod config;
